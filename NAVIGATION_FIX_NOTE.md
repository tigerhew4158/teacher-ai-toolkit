# v4.4 首页按钮连结修正版

修正内容：
- 首页导航按钮统一改成 `App.go(...)`
- 修正旧版 `go(...)`、`renderHome()` 等未导出导致点击无反应的问题
- 加入 `data-route` 与 hash link 的备用点击监听
- 首页、工具包、价格、周边、后台按钮都可正常切换
- 保留 v4.3 后台登入修正与 v4.1 切图工具功能

后台密码：
admin123
